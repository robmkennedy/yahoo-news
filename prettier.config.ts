import { type Config } from "prettier";

const config: Config = {
    tabWidth: 4,
    printWidth: 120,
    singleQuote: true,
    jsxSingleQuote: true,
    bracketSameLine: true,
    trailingComma: 'none'
};

export default config;